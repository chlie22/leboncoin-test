# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'
# Test statistics store, prepared by `make test-db` (outside var/data, which only exists in the image).
DATABASE_URL="sqlite:///%kernel.project_dir%/var/test.db"
# Small window to observe evictions (docs/conception.md §9.1).
STATS_WINDOW_SIZE=5
